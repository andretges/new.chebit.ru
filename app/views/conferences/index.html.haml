.well
  -# %p Свободная конференция веб-разработчиков, организаторами которой являются сами докладчики.
  %p Если вы хотите что-то узнать &mdash; попросите об этом рассказать, а сами сделайте доклад о том, что знаете вы.

%h2 Прошедшие события

%ul.thumbnails
  - @conferences.each do |c|
    %li.span4
      .thumbnail
        = link_to conference_path(c) do
          = image_tag c.image.extra.url, :width => '300x200'
        .caption
          %h3= link_to c.title, conference_path(c)
          %p
            %i.icon-calendar
            %small.muted= l c.date_conference
          %p= c.description.html_safe
          .pull-right
            = link_to conference_path(c), :class => 'btn' do
              %i.icon-bullhorn
              Доклады (#{c.presentations.count})
          .clearfix


-#%hr

-#.container
  -#.row
    -#- @conferences.each do |c|
      -#.span6
        -#= c.photo_content.try :html_safe

-#%br
-#%br
%hr
= render :partial => 'layouts/disquss'
