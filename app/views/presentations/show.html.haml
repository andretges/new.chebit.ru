.body-menu-block Все выступления
.row-fluid
  .span12
    .row-fluid.conf-block
      .span4
        = image_tag @presentation.image.url
      .span8.conf-view-block
        .conf-block-title
          = @presentation.title
        .conf-block-date
          %i.fontello-icon-user.icon-size-16.v-align-top
          = @presentation.autor
        .conf-block-text
          = @presentation.context
        .conf-block-text
          - if @presentation.video_embed.present?
            %i.fontello-icon-video.icon-size-16.v-align-top
          - if @presentation.file.present?
            %i.fontello-icon-doc-text-inv.icon-size-16.v-align-top

= link_to conference_path(@presentation.conference), :class => 'btn' do
  Назад